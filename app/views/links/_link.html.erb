<div>
  <p><%= truncate(link.full_link_address, length: 100)%></p>
  <p><%= "#{root_url}#{link.shortened_link_address}" %>  <span class="btn btn-info"><%= link.visits %> visits</span>
    <%= link_to "Details", link, class: "btn btn-primary" %> </p>
</div>
